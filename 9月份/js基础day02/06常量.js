var age = 10  // 变量
const name = 'tom'   // 不好的常量名，因为小写了
const SCHOOL = '传智'   // 好的常量名，推荐 用大写
name = 'lili'  // 不可以 常量
age = 20    // 可以

var age = 10  // 变量
const name = 'tom'   // 不好的常量名，因为小写了
const SCHOOL = '传智'   // 好的常量名，推荐 用大写
name = 'lili'  // 不可以 常量
age = 20    // 可以


var age = 10  // 变量
const name = 'tom'   // 不好的常量名，因为小写了
const SCHOOL = '传智'   // 好的常量名，推荐 用大写
name = 'lili'  // 不可以 常量
age = 20    // 可以

