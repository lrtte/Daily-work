1.引入CSS样式表的三种方式
1.行内式
<标签 style="css属性:属性值;…"></标签>
2.内嵌式
在head标签中写
<style>
标签名{ css属性:属性值;… }
</style>
3.外链式
样式先写在css文件中，再使用link标签引入css文件
2.块级元素的特点，行内元素的特点，行内块元素的特点分别是什么

3.标签显示模式之间的转换
1.display:inline
2.display:block
3.display:inline-block

4.CSS 三大特性分别是什么
1. 层叠性：当相同选择器中存在了相同的属性时，根据就近原则，最下面的属性值覆盖上面的属性值
2. 继承性：子元素继承父元素的某些样式，比如以text-、font-、line-等元素开头的样式，还有color样式可以被继承
3. 优先级性：在将多个选择器应用于同一个标签时，根据权重所要优先选择哪个样式，就是优先级性

5.CSS特殊性（权重）按照从小到大排序
继承(子承父)或*(通配符) < 标签选择器 < 类选择器 = 伪类选择器 < ID选择器 < 行内样式 < !important(权重无穷大)

6代码题