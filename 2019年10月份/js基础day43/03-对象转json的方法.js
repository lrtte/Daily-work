let obj={
    name:'Lisa',
    age:19,
    brother:{
        gg:'a',
        dd:'b',
        jj:'c',
        mm:'e'
    }
}
// 对象转json
JSON.stringify(对象或数组)

// let objJson=JSON.sringify(obj);
// console.log(objJson);
let arr =['a','b','c']
console.log(JSON.stringify(arr))